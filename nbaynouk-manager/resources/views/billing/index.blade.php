<x-layout title="Facturation"><x-page-header eyebrow="Finances" title="Facturation" description="Consultez les échéances et leur statut calculé automatiquement."/><div class="tabs mt-0"><a href="{{ route('billing.index') }}">Toutes</a><a href="{{ route('billing.index',['status'=>'upcoming']) }}">À venir</a><a href="{{ route('billing.index',['status'=>'partial']) }}">Partielles</a><a href="{{ route('billing.index',['status'=>'paid']) }}">Payées</a><a href="{{ route('billing.index',['status'=>'overdue']) }}">En retard</a></div><div class="table-wrap"><table><thead><tr><th>Projet</th><th>Client</th><th>Période</th><th>Montant</th><th>Payé</th><th>Reste</th><th>Échéance</th><th>Statut</th></tr></thead><tbody>@forelse($periods as $period)<tr><td><a href="{{ route('projects.show',$period->project) }}">{{ $period->project->business->name }}</a><span>{{ $period->project->code }}</span></td><td>{{ $period->project->business->client->name }}</td><td>{{ $period->period_start?->translatedFormat('F Y')??$period->description }}</td><td>{{ \App\Support\Money::format($period->amount) }}</td><td>{{ \App\Support\Money::format($period->total_paid) }}</td><td>{{ \App\Support\Money::format($period->remaining_amount) }}</td><td>{{ $period->due_date?->translatedFormat('d M Y')??'—' }}</td><td><x-badge :value="$period->payment_status"/></td></tr>@empty<tr><td colspan="8"><x-empty-state title="Aucune période" description="Les périodes créées depuis les projets apparaîtront ici."/></td></tr>@endforelse</tbody></table></div><div class="mt-8">{{ $periods->links() }}</div></x-layout>
