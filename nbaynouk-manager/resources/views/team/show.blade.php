<x-layout :title="$teamMember->name"><p class="breadcrumb"><a href="{{ route('team.index') }}">Équipe</a> / {{ $teamMember->name }}</p><x-page-header eyebrow="Membre" :title="$teamMember->name" :description="$teamMember->default_role"><x-slot:actions><form method="POST" action="{{ route('team.toggle',$teamMember) }}">@csrf @method('PATCH')<button class="button-secondary">{{ $teamMember->active?'Désactiver':'Activer' }}</button></form></x-slot:actions></x-page-header><section class="grid gap-10 lg:grid-cols-[.7fr_1.3fr]"><form class="panel space-y-5" method="POST" action="{{ route('team.update',$teamMember) }}">@csrf @method('PUT')<div><label class="label">Nom</label><input class="input w-full" name="name" value="{{ $teamMember->name }}"></div><div><label class="label">Rôle</label><input class="input w-full" name="default_role" value="{{ $teamMember->default_role }}"></div><div><label class="label">E-mail</label><input class="input w-full" type="email" name="email" value="{{ $teamMember->email }}"></div><button class="button-primary">Enregistrer</button></form><div><div class="section-heading"><h2>Projets assignés</h2></div><div class="space-y-3">@forelse($teamMember->projects as $project)<a class="watch-row border-y border-border" href="{{ route('projects.show',$project) }}"><div><p>{{ $project->name }}</p><p class="text-xs text-muted">{{ $project->business->name }}</p></div><x-badge :value="$project->status"/></a>@empty<p class="text-sm text-muted">Aucun projet assigné.</p>@endforelse</div></div></section></x-layout>
